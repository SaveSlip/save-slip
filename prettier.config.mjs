/** @type {import('prettier').Config} */
const config = {
  semi: true,
  singleQuote: false,
  quoteProps: "as-needed",
  trailingComma: "all",
  tabWidth: 2,
  useTabs: false,
  printWidth: 80,
  bracketSpacing: true,
  bracketSameLine: false,
  arrowParens: "always",
};

export default config;
